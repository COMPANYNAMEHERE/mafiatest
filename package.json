{
  "name": "empire-noir",
  "version": "0.1.0",
  "private": true,
  "description": "Empire Noir – a 1920s Mafia incremental strategy web game",
  "repository": {
    "type": "git",
    "url": "https://github.com/COMPANYNAMEHERE/mafiatest.git"
  },
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*",
    "infra"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "start": "pnpm -r start"
  },
  "dependencies": {
    "pnpm": "^10.13.1"
  },
  "devDependencies": {
    "eslint": "^8.20.0",
    "prettier": "^2.8.0",
    "typescript": "^5.0.0"
  }
}
